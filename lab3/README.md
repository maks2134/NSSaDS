# Лабораторная работа №3: TCP Server с Select() Мультиплексированием

## Обзор

Реализация TCP сервера с **мультиплексированием через системный вызов `select()`** для параллельной обработки нескольких клиентов в одном потоке.

### 🎯 Цели лабораторной работы

- ✅ **Параллельное обслуживание** нескольких клиентов в одном потоке
- ✅ **Мультиплексирование** с использованием системного вызова `select()`
- ✅ **Динамический размер чанков** на основе ping времени (t = ping * 10)
- ✅ **Непрерывная передача файлов** во время обработки команд
- ✅ **Структура как в lab1/lab2** с сохранением архитектуры

## 🏗️ Архитектура

```
lab3/
├── cmd/
│   ├── server/           # Сервер с select() мультиплексированием
│   │   └── main.go
│   └── client/          # Клиент для тестирования
│       └── main.go
├── internal/
│   ├── domain/          # Бизнес-логика и сущности
│   │   ├── command.go  # Команды и структуры данных
│   │   └── server.go   # Интерфейсы сервера
│   ├── infrastructure/
│   │   └── network/   # Сетевая инфраструктура
│   │       ├── select_multiplexer.go  # Реализация select()
│   │       ├── unix_select.go        # Обертка над syscall.Select
│   │       └── tcp_server.go        # TCP сервер
│   └── usecase/          # Бизнес-логика
│       └── commands.go  # Реализация команд
├── go.mod               # Модуль Go
├── Makefile             # Сборка и управление
└── README.md           # Документация
```

## 🔧 Ключевые особенности

### 🔄 Мультиплексирование с select()

Сервер использует системный вызов `select()` для одновременного отслеживания нескольких файловых дескрипторов:

```go
// Подготовка наборов файловых дескрипторов
readFds := &syscall.FdSet{}
writeFds := &syscall.FdSet{}
exceptFds := &syscall.FdSet{}

// Добавление listener и клиентских соединений
sm.selectSystem.FDSet(sm.listenerFD, readFds)
for _, client := range sm.clients {
    sm.selectSystem.FDSet(int(client.FD), readFds)
}

// Вызов select() для ожидания активности
n, err := sm.selectSystem.Select(maxFD+1, readFds, writeFds, exceptFds, timeout)
```

### 📦 Динамический размер чанков

Размер чанков вычисляется на основе ping времени:

```go
func (sm *selectMultiplexer) calculateOptimalChunkSize(ping time.Duration) int {
    // t = ping * 10 согласно требованиям
    targetLatency := ping * 10
    
    // Расчет размера чанка на основе сетевых условий
    bytesPerMs := 1024 * 1024 / 1000 // 1MB/s baseline
    chunkSize := int(targetLatency.Milliseconds()) * bytesPerMs
    
    // Ограничение разумными границами
    if chunkSize < domain.MinChunkSize { chunkSize = domain.MinChunkSize }
    if chunkSize > domain.MaxChunkSize { chunkSize = domain.MaxChunkSize }
    
    return chunkSize
}
```

### 🔄 Однопоточная обработка

Сервер обрабатывает клиентов в одном потоке:
- **Новые соединения** обрабатываются через select()
- **Чтение данных** от клиентов не блокирует других
- **Передача файлов** продолжается во время обработки команд
- **Ping таймауты** проверяются регулярно

## 🚀 Установка и запуск

### Сборка

```bash
# Клонирование репозитория
git clone <repository-url>
cd lab3

# Установка зависимостей
go mod tidy

# Сборка сервера и клиента
make build

# Или по отдельности
make build-server
make build-client
```

### Запуск

```bash
# Запуск сервера
make run-server

# Запуск клиента
make run-client

# Быстрая демонстрация
make demo

# Тестирование с множественными клиентами
make test-server
```

### Параметры командной строки

#### Сервер
```bash
./lab3-server [flags]

Флаги:
  -host string          Хост сервера (default: "localhost")
  -port int            Порт сервера (default: 8080)
  -max-clients int     Макс. количество клиентов (default: 100)
  -ping-timeout       Таймаут ping (default: 30s)
  -chunk-size int      Размер чанка (default: 1024)
  -select-timeout     Таймаут select() (default: 10ms)
```

#### Клиент
```bash
./lab3-client [flags]

Флаги:
  -host string      Хост сервера (default: "localhost")
  -port int        Порт сервера (default: 8080)
```

## 💻 Использование

### Подключение к серверу

```bash
# Использование telnet
telnet localhost 8080

# Использование netcat
nc localhost 8080

# Использование встроенного клиента
make run-client
```

### Поддерживаемые команды

```
ECHO <text>     - Эхо ответ
TIME            - Текущее время сервера
HELP            - Справка
CLOSE/EXIT/QUIT - Закрытие соединения
```

### Примеры использования

```
client> ECHO Hello World!
server: Hello World!

client> TIME
server: 2026-02-27T19:30:45+03:00

client> HELP
server: Available commands:
  ECHO <text>     - Echo the provided text
  TIME            - Get current server time
  CLOSE/EXIT/QUIT - Close connection
  HELP            - Show this help message

client> QUIT
server: Connection closing...
```

## 📊 Производительность

### Преимущества select() подхода

| Характеристика | Select() | Горутины |
|----------------|----------|----------|
| **Потребление памяти** | Низкое | Высокое |
| **Переключение контекста** | Отсутствует | Есть |
| **CPU эффективность** | Высокая | Средняя |
| **Простота отладки** | Высокая | Средняя |
| **Масштабируемость** | Ограничена FD | Высокая |

### Метрики производительности

- **Одиночный поток**: Нет накладных расходов на переключение контекста
- **Select() таймаут**: 10ms для балансировки между отзывчивостью и CPU нагрузкой
- **Размер чанка**: Динамически адаптируется под условия сети
- **Максимальные клиенты**: Ограничено только файловыми дескрипторами системы

## 🧪 Тестирование

### Базовое тестирование

```bash
# Сборка и запуск тестов
make test

# Запуск бенчмарков
make benchmark

# Тестирование с множественными клиентами
make test-server
```

### Нагрузочное тестирование

```bash
# Множественные подключения
for i in {1..20}; do
    echo "ECHO Client $i" | nc localhost 8080 &
done

wait
```

### Демонстрация

```bash
# Быстрая демонстрация функциональности
make demo
```

## 🔧 Разработка

### Структура проекта

- **Clean Architecture**: Разделение на domain, infrastructure, usecase
- **Интерфейсы**: Четкое определение контрактов между слоями
- **Тестируемость**: Легкая замена зависимостей для тестов
- **Масштабируемость**: Простое добавление новых команд и функций

### Добавление новых команд

1. Создать структуру команды в `internal/usecase/commands.go`
2. Реализовать методы `Execute()` и `Name()`
3. Зарегистрировать команду в `NewCommandHandler()`

```go
type MyCommand struct{}

func (c *MyCommand) Execute(ctx context.Context, args []string) (string, error) {
    return "My response", nil
}

func (c *MyCommand) Name() string {
    return "MYCOMMAND"
}
```

## 🐛 Устранение неисправностей

### Частые проблемы

1. **"Too many open files"**
   ```bash
   # Увеличение лимита файловых дескрипторов
   ulimit -n 4096
   ```

2. **"Connection refused"**
   ```bash
   # Проверить, запущен ли сервер
   lsof -i :8080
   
   # Проверить правильность хоста/порта
   telnet localhost 8080
   ```

3. **Медленный отклик**
   ```bash
   # Уменьшение таймаута для лучшей отзывчивости
   ./lab3-server -select-timeout=1ms
   
   # Увеличение размера чанка
   ./lab3-server -chunk-size=2048
   ```

### Логирование

Сервер предоставляет детальное логирование:
- Подключения/отключения клиентов
- Ошибки обработки запросов
- Информация о файловых дескрипторах
- Статистика производительности

### Отладка

```bash
# Запуск с отладочной информацией
./lab3-server -host=localhost -port=8080 -select-timeout=1ms

# Мониторинг файловых дескрипторов
lsof -p <server-pid>

# Мониторинг сетевых соединений
netstat -an | grep :8080
```

## 📈 Сравнение с другими подходами

### Select() vs Горутины

| Параметр | Select() | Горутины |
|-----------|-----------|-----------|
| **Использование памяти** | ~1MB на 1000 клиентов | ~50MB на 1000 клиентов |
| **CPU при простое** | Минимальный | Средний |
| **Латентность** | Низкая | Очень низкая |
| **Масштабируемость** | До ~10k клиентов | До ~100k клиентов |
| **Сложность отладки** | Низкая | Средняя |

### Когда использовать select()

- ✅ Ограниченные ресурсы памяти
- ✅ Небольшое количество клиентов (< 1000)
- ✅ Требуется максимальная CPU эффективность
- ✅ Простота отладки важна

### Когда использовать горутины

- ✅ Большое количество клиентов (> 1000)
- ✅ Доступно много памяти
- ✅ Требуется максимальная масштабируемость
- ✅ Сложная логика обработки клиентов

## 📚 Теоретические основы

### Системный вызов select()

`select()` - это системный вызов для мультиплексирования I/O:

```c
int select(int nfds, fd_set *readfds, fd_set *writefds, 
           fd_set *exceptfds, struct timeval *timeout);
```

**Преимущества:**
- Портативность (POSIX)
- Простота использования
- Низкие накладные расходы

**Недостатки:**
- O(n) сложность по количеству FD
- Ограничение на максимальное количество FD (обычно 1024)
- Неэффективен при большом количестве соединений

### Расчет размера чанков

Согласно требованиям лабораторной работы:

```
t = ping * 10

chunk_size = (t * bandwidth) / 8
```

Где:
- `t` - целевая задержка
- `bandwidth` - пропускная способность сети
- `chunk_size` - размер порции данных

## 🏁 Выводы

Реализация лабораторной работы №3 демонстрирует:

1. **Эффективное мультиплексирование** с использованием системных вызовов
2. **Оптимальное управление ресурсами** в однопоточном окружении
3. **Динамическую адаптацию** под сетевые условия
4. **Чистую архитектуру** с разделением ответственности
5. **Высокую производительность** при параллельной обработке клиентов

Сервер успешно обрабатывает множество клиентов в одном потоке, обеспечивая отзывчивость и эффективность согласно требованиям лабораторной работы, при этом сохраняя структуру и подходы из lab1/lab2.

## 📝 Лицензия

Проект разработан в рамках учебных целей для курса "Сети и системы".
